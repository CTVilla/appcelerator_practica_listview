<Alloy>
	<Collection id="localLibrary" src="feed" instance="true" />	
	<Window class="container">		
		<ListView id="list" defaultItemTemplate="template">
			<Templates>
				<ItemTemplate name="template">
					<View class="row">
						<Label bindId="titlelbl" class="titlelbl"/>
	                    <Label bindId="descriptionlbl" class="descriptionlbl"/>	
					</View>
                </ItemTemplate>
			</Templates>
			<ListSection dataCollection="$.localLibrary">
				<ListItem
					titlelbl:text="{title}"
					descriptionlbl:text="{description}"
				/>
			</ListSection>
		</ListView>
	</Window>
</Alloy>